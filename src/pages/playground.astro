---
import Layout from '~/layouts/PageLayout.astro';
import Playground from '~/components/widgets/Playground.astro';

const metadata = {
  title: 'Playground — DNA Codes',
  description:
    'Feed DNA Codes your knowledge and watch it compile into a living spec — plus Process Flow, SOP, Agent Workflow, and App artifacts.',
  ignoreTitleTemplate: true,
};
---

<Layout metadata={metadata}>
  <Playground />
</Layout>
