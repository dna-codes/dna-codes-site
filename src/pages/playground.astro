---
import Layout from '~/layouts/PageLayout.astro';
import Playground from '~/components/widgets/Playground.astro';

const metadata = {
  title: 'Playground — DNA Codes',
  description:
    'Paste a transcript and watch DNA Codes turn it into a living spec — plus Process Flow, RACI, SOP, and Runbook artifacts.',
  ignoreTitleTemplate: true,
};
---

<Layout metadata={metadata}>
  <Playground />
</Layout>
